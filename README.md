# ME193-Code

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Structure

Project work is organized into dated subfolders (e.g. `9/15` for September 15).
